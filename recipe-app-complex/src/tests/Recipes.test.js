// import { screen } from '@testing-library/react';
// import App from '../App';
// import { renderWithRouterAndRedux } from './Helpers/renderWithRouterAndRedux';

// describe('Tentando os categorias', () => {
//   const PAGE_TITLE = 'category-filter';

//   test('Testando os inputs são renderizados', () => {
//     renderWithRouterAndRedux(<App />, {}, '/meals');

//     const pageTitle = screen.getByTestId(PAGE_TITLE);
//     expect(pageTitle).toBeVisible();
//     expect(pageTitle).toHaveTextContent('Meals');

//     const pagetittle = screen.getByTestId(PAGE_TITLE);
//     expect(pagetittle).toBeVisible();

//     const searchButton = screen.getBytTestId(PAGE_TITLE);
//     expect(searchButton).toBeVisible();
//     expect(searchButton).getByRole('button');
//   });
// });
